{
  "name": "cordova-plugin-downloader",
  "version": "3.0.0-dev",
  "description": "Cordova downloader Plugin",
  "types": "./types/index.d.ts",
  "cordova": {
    "id": "cordova-plugin-downloader",
    "platforms": [
      "android"
    ]
  },
  "keywords": [
    "cordova",
    "downloader",
    "ecosystem:cordova",
    "cordova-android"
  ],
  "scripts": {
    "test": "npm run lint",
    "lint": "eslint ."
  },
  "engines": {
    "cordovaDependencies": {
      "0.1.0": {
        "cordova": ">=3.0.0"
      },
      "3.0.0": {
        "cordova": ">100"
      }
    }
  },
  "devDependencies": {
    "@cordova/eslint-config": "^3.0.0"
  }
}